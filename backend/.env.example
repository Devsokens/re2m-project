# Supabase project settings (Project Settings > API in the Supabase dashboard)
SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...
SUPABASE_ANON_KEY=eyJ...

# Storage bucket used for all uploaded images (member photos, CMS images, logos, stamp, covers)
SUPABASE_STORAGE_BUCKET=public-assets

# Comma-separated list of origins allowed to call this API (your Vercel frontend URL + localhost for dev)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,https://your-frontend.vercel.app

# Port Render (or your local machine) should run the server on
PORT=4000

# Gmail API (OAuth2) — used to send request/confirmation emails without SMTP
# (Render blocks outbound SMTP ports, HTTPS calls to the Gmail API work fine).
# Optional: leave blank and email sending is skipped with a console warning.
# See backend/README.md section 5 for how to obtain these.
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=
GMAIL_SENDER_EMAIL=
GMAIL_SENDER_NAME=Cabinet RE2M
