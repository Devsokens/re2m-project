# URL of the backend API (see backend/README.md). Defaults to http://localhost:4000
# when unset, which is fine for local dev. Set this in Vercel's project settings
# for the deployed frontend, pointing at your Render backend URL.
VITE_API_URL=http://localhost:4000
